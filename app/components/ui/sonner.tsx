"use client";

// TODO: Install next-themes to enable Toaster component
// import { useTheme } from "next-themes";
import { ToasterProps } from "sonner";

const Toaster = ({ ...props }: ToasterProps) => {
  // Stub component - install next-themes to enable full functionality
  // Run: npm install next-themes
  return null;
};

export { Toaster };
